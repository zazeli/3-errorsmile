{
  "name": "3-errorsmile",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": { 
    "dev": "live-server --port=5000 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andzelika",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
